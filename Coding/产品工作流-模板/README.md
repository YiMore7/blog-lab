# 🚀 AI 驱动的产品经理工作流 (PM-Workspace)

本项目是一个基于 Trae Agent 的、从“想法”到“原型”的自动化产品管理工作流。

## 🎯 工作流概览

本工作流通过三个核心 Agent，将产品经理的工作分为三个标准阶段，实现结构化、可追溯和高效率的价值交付。

**`Idea` -> `Problem Statement` -> `PRD` -> `Prototype`**

---

## 📂 目录结构与逻辑

* `/agents/`: 存放所有 Agent 的 "System Prompt" (大脑/逻辑)。
* `/templates/`: 存放所有标准产出物的 "模板" (骨架/结构)。
* `/ideas/`: **[阶段1产出]** 存放经过压力测试的 "问题陈述" (`problem_statement_xxx.md`)。
* `/prds/`: **[阶段2产出]** 存放生成的 "PRD文档" (`xxx_prd.md`)。
* `/prototypes/`: **[阶段3产出]** 存放 "可交互原型" (`/xxx_demo/`)。

---

## 🤖 核心 Agents

1.  **`@PM_Pressure_Tester` (认知压力测试员)**
    * **职责：** (阶段1) 挖掘需求的本质，对Idea进行压力测试。
    * **命令：** `/summarize [output_path]` (用于总结讨论，生成问题陈述)

2.  **`@PRD_Generator` (PRD撰写助手)**
    * **职责：** (阶段2) 基于问题陈述，填充PRD模板，生成PRD初稿。
    * **命令：** `/generate [input_problem_path] [output_prd_path]`

3.  **`@Prototype_Builder` (原型构建师)**
    * **职责：** (阶段3) 基于PRD中的选中内容，生成原型。
    * **命令 (高保真)：** `/build [output_dir]`
    * **命令 (低保真)：** `/flow [target_prd_file]`

---

## 📖 [SOP] 新功能开发标准操作步骤

**阶段 1：定义问题 (Idea -> Problem Statement)**
1.  **调用Agent：** `@PM_Pressure_Tester`
2.  **输入Idea：** `“我有一个新想法...（描述你的idea）”`
3.  **交互：** 与Agent进行多轮问答。
4.  **执行命令：** （讨论充分后）
    `/summarize ideas/problem_statement_[feature_name].md`
5.  **产出：** `ideas/problem_statement_[feature_name].md`

**阶段 2：撰写方案 (Problem Statement -> PRD)**
1.  **调用Agent：** `@PRD_Generator`
2.  **执行命令：**
    `/generate ideas/problem_statement_[feature_name].md prds/[feature_name]_prd.md`
3.  **产出：** `prds/[feature_name]_prd.md`
4.  **迭代：** （可选）在聊天中继续要求Agent修改PRD。

**阶段 3：构建原型 (PRD -> Prototype)**
1.  **调用Agent：** `@Prototype_Builder`
2.  **打开PRD文件** 并 **高亮** 想生成原型的部分。
3.  **执行命令：**
    * `/build prototypes/[feature_name]_demo` (高保真)
    * `/flow prds/[feature_name]_prd.md` (低保真流程图)
4.  **产出：** 可交互原型或流程图。
5.  **预览：** 使用 `Live Preview` 插件查看并迭代。